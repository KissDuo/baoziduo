// Mobile-specific inner layout.
// The main shell (header + bottom tab) is provided by the root layout.
export default function MobileLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
